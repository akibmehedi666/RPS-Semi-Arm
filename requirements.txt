# Core Runtime Dependencies (Tested on Laptop x86_64 & Raspberry Pi 5 aarch64)
numpy>=1.24.0,<3.0.0
torch>=2.0.0
torchvision>=0.15.0

# OpenCV: use opencv-python for desktop GUI / HUD, or opencv-python-headless for headless Pi setups
opencv-python>=4.8.0

# Ultra-fast inference engine for ARM Cortex-A76 (Pi 5) and x86_64
onnxruntime>=1.15.0
onnx>=1.14.0
onnxscript>=0.1.0

# Training & Offline Metrics (optional if only running live prediction on the Pi)
scikit-learn>=1.2.0
matplotlib>=3.7.0
